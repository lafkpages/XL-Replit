name: 'TODO Comments'
on: ['push']
jobs:
  build:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: 'actions/checkout@master'
      - uses: 'buluma/todo-to-issue-action@v4.6.2'
        id: 'todo'
